# 奶油风 HyperFrames 动效模板

## 适用场景

适合中文真人口播视频的辅助动效。动效放在人物两侧或人物周围，必须保留中间人物口播区域，不遮挡脸部、嘴部和主要动作。

## 画面结构

- 左侧：一组大字，负责说清楚当前重点。
- 中间：留给人物口播，保持干净，不放大面积面板。
- 右侧：用 2 到 3 个小卡片、缩略图、标签或图形演示对应概念。
- 每个画面只表达一个重点，文字不要整段堆叠。
- 小字优先拆成两行，避免一整行横向挡住人物。

## 奶油风视觉规范

- 背景：透明优先；如果需要底色，使用奶油白 `#F7F0E5`。
- 主文字：深咖啡 `#3B352F`。
- 高亮色：珊瑚橙 `#E58B76`、鼠尾草绿 `#A9B8A0`、柔雾粉 `#D99A9B`、奶油黄 `#E9C875`。
- 禁用：深蓝、纯黑大底、厚重黑色大盒子、强烈霓虹渐变。
- 字体：思源黑体/Source Han Sans SC；中文主标题使用常规或中等字重，不默认加粗。
- 文字效果：使用柔和投影增强可读性，不加黑色底条。
- 卡片：奶油白或低饱和色块，细深咖啡描边，圆角约 14 到 18px，轻微纸张阴影。
- 图形：采用纸张、文件、标签、时间轴、缩略图、贴纸等轻量信息图形，不使用大面积 UI 面板。

## 动效节奏

1. 大字从左侧轻微滑入，约 0.4 到 0.6 秒。
2. 右侧第一个卡片从上方落下，带轻微回弹。
3. 第二个卡片从侧面弹出，第三个标签或数据贴纸随后出现。
4. 卡片之间错开 0.12 到 0.2 秒，形成连续的手工拼贴感。
5. 时间轴、数字或进度元素可以从 0% 平滑增长到目标值。
6. 画面切换使用短暂淡出、横向擦拭或卡片滑移，避免突然跳切。
7. 结束时保留最后一帧约 0.5 秒，方便与人物口播衔接。

## 文案拆分规则

- 原始长文案先按语义拆成 2 到 4 个画面。
- 每个画面只保留一个大关键词和一条短辅助说明。
- 大字示例：`AI剪辑`、`自动整理`、`一键成片`。
- 小字示例：`素材 → 字幕 → 动画`，拆成两行时写成：`素材 → 字幕` / `动画 → 成片`。
- 数字、产品名和核心结论可以单独做成贴纸或卡片。

## 推荐画面模板

### 画面 1：主题出现

- 左侧大字：`[核心主题]`
- 右侧：文件卡片 + 标签卡片
- 动效：标题滑入，文件卡片下落回弹，标签轻轻弹出

### 画面 2：过程演示

- 左侧大字：`[关键动作]`
- 右侧：时间轴卡片 + 2 个小步骤卡片
- 动效：时间轴进度增长，步骤卡片依次出现

### 画面 3：结果强调

- 左侧大字：`[最终结果]`
- 右侧：完成标签、导出文件卡片或数据贴纸
- 动效：结果卡片缩放进入，最后的标签轻微旋转后稳定

## 复用时的输入格式

请提供：

```text
文案：……
画幅：16:9 / 9:16 / 透明底叠加
时长：未指定时按文案自动判断
人物位置：中间 / 左侧 / 右侧
重点词：……
```

如果没有特别说明，默认采用本模板的奶油风配色、透明底优先、左侧大字、右侧小卡片动效，并自动避开人物脸部和口播区域。
