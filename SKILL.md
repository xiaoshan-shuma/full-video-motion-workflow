---
name: full-video-motion-workflow
description: "当用户提供口播视频、配音、录屏、图片或其他素材，并希望自动完成整条视频的分镜、动效、B-roll、转场、音效、BGM和 ChatCut 成片时使用。支持在小珊深海军蓝科技风与奶油拼贴风之间选择，并由 HyperFrames/Remotion 制作独立动态素材后导入 ChatCut。"
metadata:
  short-description: "按完整分镜制作科技蓝或奶油风口播视频"
---

# 全流程 AI 视频制作

## 目标

把用户提供的素材做成一条完整、可播放、可继续编辑的 ChatCut 视频。默认以真人口播或已有配音为 A-roll 主线，以 HyperFrames 和 Remotion 制作独立的动态 B-roll、Motion Graphics 和转场，再在 ChatCut 中组合、混音、检查和交付。

核心单位是**整条视频的视觉段落**，不是“一句口播配一个静态画面”。每个段落都必须有进入、发展、重点、退出，并和前后段落有明确的运动承接。

## 自动执行规则

- 用户说“生成视频”“开始做”“继续做”“按这个素材完成”时，直接执行完整流程，不停在口头方案；只有用户明确说“先设计”“先给方案”时，才只输出分镜方案。
- 优先使用用户当前打开的 ChatCut 项目和当前时间线，不擅自创建新项目、改其他项目或使用旧版本素材。
- 除非用户明确要求导出，完成标准是 ChatCut 中可播放、可继续编辑的时间线；用户说“导出”或“保存到桌面”后，才导出并交付文件。
- 用户已经有字幕时，保留字幕并禁止重复生成；用户说不要字幕时，完全跳过字幕制作。
- 用户已有配音时，默认保留原配音；不擅自重新配音、克隆声音或改写口播。
- 用户已有 BGM 时，只调整该 BGM 的音量，不改其他音轨。对本用户的口播项目，默认先以约 `-30 dB` 作为人声下的 BGM 起点，再按试听微调。
- 不擅自增加额外版本、额外语言、额外 BGM 或额外音效。

## 视觉风格选择

每次生成前先确定一种全片主风格，不要在同一条视频里无理由混用两套色板。调用时支持以下二选一：

1. **小珊深海军蓝科技风**：适合工具、流程、数据和产品讲解。读取 [小珊视觉风格](references/xiaoshan-style.md)，除非用户明确选择奶油风，否则默认使用这一风格。
2. **奶油拼贴风**：适合更温暖、生活化、轻量的中文口播和知识表达。读取 [奶油视觉风格](references/cream-style.md)。

如果用户没有选择风格，先询问“这次用科技蓝还是奶油风？”；如果用户说“按默认”或没有时间等待确认，使用科技蓝。选定后整条视频沿用该风格的色板、卡片、字体和动效节奏。

### 科技蓝共同执行约束

选择科技蓝后：

- 16:9 横屏，默认 `1920×1080`；用户明确指定尺寸或比例时以本次要求覆盖。
- 深海军蓝背景和面板，青蓝作为结构线和高光，粉色作为关键词/重点动作色，黄色只做少量强调。
- 左侧承担大标题和主叙事，右侧承担 UI、卡片、流程或数据；不要把画面做成纯网页截图。
- 中文是第一视觉层级；英文只作为产品名、系统标签、步骤码或小号辅助信息。
- 大标题要清晰、有描边或高对比边缘，不得乱码、重叠、裁切或无故换行。
- 进度条是全片唯一的全局顶部层：底轨不透明、细、清晰，激活段用青蓝或浅色显示，不使用红色标注线，不为每个段落额外画一条横线。
- 动效背景必须真的透明；文字、图形、卡片和边框保持不透明。普通 H.264/MP4 黑底不能冒充透明素材。
- 人物脸部、眼睛、嘴巴和字幕区域属于安全区。动效不能盖脸；出现 B-roll 时，人物可缩小成带圆形遮罩的画中画，默认选择左下角，若字幕或构图冲突则改为右下角。

### 奶油风共同执行约束

选择奶油风后：

- 背景优先透明；需要底色时使用奶油白 `#F7F0E5`，主文字使用深咖啡 `#3B352F`。
- 高亮从珊瑚橙 `#E58B76`、鼠尾草绿 `#A9B8A0`、柔雾粉 `#D99A9B`、奶油黄 `#E9C875` 中选择，每个画面控制在 2–3 个强调色。
- 使用 14–18px 圆角、细深咖啡描边和轻纸张阴影；避免深蓝、纯黑大底、厚重黑盒和霓虹渐变。
- 默认左侧放一个大关键词，右侧放 2–3 个小卡片/标签；人物区域保持干净，不遮挡脸、嘴、字幕和主要动作。
- 标题轻微滑入，卡片以 0.12–0.20 秒错峰下落或侧向弹出，形成轻手工拼贴感；不要使用扫描线、强闪白或夸张弹跳。
- 叠加素材必须是真透明格式；具体色板、版式、文案拆分和节奏读取 [奶油视觉风格](references/cream-style.md)。

## 完整执行顺序

### 1. 盘点项目和素材

先读取项目地图和当前时间线，再检查素材：

1. 确认项目、当前时间线、画布、帧率和总时长。
2. 把素材分为 A-roll、B-roll、图片、Motion Graphics、字幕、音效和 BGM。
3. 确认人物视频是否已有声音、字幕是否已存在、BGM 位于哪条轨道。
4. 检查是否已有可复用的进度条、人物圆形画中画、标题、卡片、数字、Logo 或转场素材。
5. 如果字幕或配音已经存在，先保护它们，不因后续生成而覆盖。

工具路由：用 `read_project`、`preview_timeline`、`inspect_item`、`browse_assets` 和 `inspect_asset` 做盘点；需要按口播内容决定“播放什么”时使用 Script，而不是用时间戳硬切语音。

### 2. 分析整条口播并建立分镜

先完整理解整条视频，再制作任何动效。分镜按画面变化、观众理解任务和动作节奏拆分，不按平均时长切割。

每个镜头必须写清：

- `sceneId`、起止时间、对应口播和语义目的
- 画面类型：A-roll、全屏 B-roll、透明叠加、画中画、UI/卡片或收尾 CTA
- 进场动作、主体运动、关键词 cue、稳定态和退场动作
- 与前后镜头的连接方向、转场长度和转场素材
- 标题/卡片/数字/Logo 的位置、大小和层级
- 人物安全区、字幕安全区和顶部进度条状态
- 是否需要音效、音效种类、音效锚点和 BGM 音量

连续几句可以合成一个视觉段落；一句话也可以拆成多个镜头。每个视觉段落至少有一个有意义的运动变化，但不要把每个小元素都做成独立闪烁。

把分镜保存为 `scene-plan.json`，同时生成一份人类可读的 `STORYBOARD.md`。推荐字段和示例见 [分镜模板](references/scene-plan-template.md)。

### 3. 先制作独立动态素材

严格区分工具职责：

- **HyperFrames**：网页/产品操作、鼠标、滚动、翻页、按钮、卡片、流程、浏览器感界面和交互演示。
- **Remotion**：全局进度条、标题系统、数据滚动、人物圆形画中画变换、跨镜头时间编排、批量可复用组件和自定义转场。
- **ChatCut**：导入、排列、缩放、裁剪、轨道管理、原有字幕、BGM、音效、人声处理和最终导出。

需要叠加到人物上的动效，优先在 HyperFrames/Remotion 中导出透明 WebM VP8/VP9 或 ProRes 4444。全屏不透明 B-roll 才使用普通 MP4。导出后先在透明检查底图上确认 Alpha 存在，再放入 ChatCut。

每个动态元素独立成素材，至少保持这些独立文件：

- `progress-bar`：全片一个全局层
- `title`：大标题和关键词强调
- `metric-counter`：播放量/涨粉等滚动数字
- `feature-card`：字幕、剪辑、包装等卡片
- `logo-drop`：用户提供的官方 Logo 下落/回弹
- `speaker-bubble`：人物缩小成圆形画中画
- `scene-transition`：单独的转场素材或 ChatCut 过渡

不要把所有效果合成一个大视频。一个镜头有多个元素时，仍然分别制作，以便后续单独调位置、时长和大小。

用户提供官方 Logo 或参考图时，优先使用用户给的图像资产，不凭记忆重画 Logo；需要裁切、去背景或透明化时，先保留原图副本。

### 4. 为所有镜头设计转场

每一个相邻镜头都要有转场策略，不能只在最后随意加一个淡化。转场应继承前镜头的运动方向或视觉形状：

- 左右平移接左右平移
- 推近接局部放大或 UI 展开
- 卡片出现接卡片翻页或同色延展
- 数据滚动接数字/图表变化
- 人物全屏接人物圆形缩小
- 同一条线、同一块面板或同一颜色可以作为视觉桥接

默认转场长度为 6–12 帧，使用 ease-out、ease-in-out 或轻微 spring。回弹要有减速和余韵，不能像硬碰撞；除非用户特别要求，不使用夸张弹跳、闪白或黑场。

在 ChatCut 中需要真实剪辑边界的转场，使用 `edit_item` 的 transition；需要自定义图形运动的转场，在 HyperFrames/Remotion 单独输出透明素材。不要用黑色全屏层伪装转场。

### 5. 导入和组装 ChatCut

按 [ChatCut 组装清单](references/chatcut-runbook.md) 执行：

- 主人物口播放在主视频轨。
- 全屏 B-roll 放在替换画面轨。
- 透明标题、卡片、数字和装饰放在独立上层轨。
- 顶部进度条放在最高的全局包装轨，覆盖全片且只保留一条。
- 人物画中画使用圆形遮罩和安全边距，位于不挡字幕和关键内容的角落。
- 用户已有字幕保持原样，不另建字幕轨。
- 音效和 BGM 与画面分轨，避免把音效烙进每个动态素材里。

先导入并核对全部素材，再按 `scene-plan.json` 和 `chatcut-import-plan.json` 放置。素材放错、黑底、乱码或挡脸时，只修正对应素材，不重新破坏整条时间线。

### 6. 音频处理

- 人物口播含有人声时，检查并在用户没有明确拒绝的情况下做人声加强/语音隔离；只作用于说话的素材，不作用于 BGM、环境声或音效。
- 原配音保持原内容和顺序，不因人声处理而重新生成。
- BGM 置于独立轨道，先降低到不会盖住人声的水平；本用户项目可从 `-30 dB` 起步。
- `Mouse Click` 只放在真实的页面切换、翻页或主要视觉段落切换点；不要每出现一个小图标就响一次。
- whoosh 用于明确的平移/翻页，pop 用于卡片弹出，impact 只用于少量重点强调。没有必要时不增加音效。
- 音效必须留出尾音，不得和下一个音效重叠，也不得延长视频总时长。

### 7. 逐镜头验收

每个镜头至少检查：首帧、进入 cue 前后、主体稳定态、转场中段、转场末帧和退出后的下一镜头。

必检项目：

- 每个视觉段落都有运动变化，不出现长时间无意义冻结。
- 每次切换都有自然承接，没有突然硬切、黑帧或方向冲突。
- 顶部进度条全片存在、底轨不透明、足够细、不用红线、不挡内容。
- 动效透明区域真的透明，黑底没有混入；文字和图形保持清晰不透明。
- 人物脸部、字幕和主要动作没有被覆盖。
- 大字没有乱码、重叠、裁切或错误换行。
- 画中画圆形大小稳定，位置不在不同镜头间无故跳动。
- 用户提供的字幕没有被重复生成、替换或遮挡。
- 人声清楚，BGM 不盖人声，音效只在必要的动作点出现。
- 轨道、素材、时长和间隙没有意外变化。

必须结合实际 viewer 画面验证，不能只凭“导入成功”或工具返回成功就宣布完成。发现问题时只改对应镜头或素材。

### 8. 交付和保留

默认保留以下可复用资料：

- `scene-plan.json`
- `STORYBOARD.md`
- `asset-manifest.json`
- `chatcut-import-plan.json`
- `audio-plan.json`
- `style-tokens.json`
- `qa-report.md`
- HyperFrames/Remotion 的源文件和透明输出文件

除非用户要求导出，否则把可编辑结果留在 ChatCut 项目中。用户要求导出时，先导出完整视频，再按用户要求交付；用户说保存到桌面时，确认下载完成后再复制到桌面，不凭文件名猜测下载是否成功。

## 不可违反的边界

- 不把一句口播机械配成一个静态画面。
- 不把所有动效合并成一个难以调整的大视频。
- 不用黑底 MP4 代替透明素材。
- 不在 ChatCut 内偷偷替代用户明确要求的 HyperFrames/Remotion 制作环节。
- 不重复添加已有字幕，不擅自改人物、配音、BGM 或用户已经确认的画面。
- 不用用户未提供的 Logo 版本，不凭记忆制作近似 Logo。
- 不把进度条做成宽色块、红色标记线或每段一条孤立横线。
- 不为了“有动效”而让元素盖住脸、字幕或主要信息。

## 用户下次可以直接说

```text
请使用“全流程AI视频制作”完成这条视频。
读取我当前 ChatCut 项目里的素材，先完整分析 A-roll、B-roll、字幕和音频，
然后为整条视频建立分镜，不要一句口播只配一个画面。
使用 HyperFrames/Remotion 分别制作每个镜头需要的透明动效、B-roll、转场和进度条，
每个效果单独成素材，再全部导入 ChatCut 组合。
采用小珊的深海军蓝、青蓝、粉色、少量黄色风格（或改用奶油拼贴风）；顶部保留一条不透明的细进度条，
人物需要时缩小成不挡脸的圆形画中画，保留我已有的字幕，不要重复加字幕。
每个镜头都要有进入、发展、重点和退出，镜头之间要有丝滑转场。
音效只放在主要画面切换/翻页节点，BGM 不盖人声；完成后在 ChatCut 里逐镜头检查。
除非我说导出，否则先完成可编辑的 ChatCut 时间线。
```
