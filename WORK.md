# 全流程 AI 视频制作：使用工作文档

## 1. Skill 信息

- 用户可读名称：全流程 AI 视频制作
- 技术标识：`full-video-motion-workflow`
- GitHub：https://github.com/xiaoshan-shuma/full-video-motion-workflow
- 开源协议：MIT
- 当前状态：公开、可安装、可直接调用

这个 Skill 用于把真人口播、录屏、图片或混合素材制作成完整、可播放、可继续编辑的 ChatCut 视频。它先理解整条内容，再按视觉段落设计分镜、动效、B-roll、转场和声音，最后在 ChatCut 中组装和验收。

核心原则：不要把“一句口播”机械地做成一张静态画面；每个视觉段落都要有进入、发展、重点、稳定态、退出和自然转场。

## 2. 安装与调用

### 一句话安装

在终端执行：

```bash
curl -fsSL https://raw.githubusercontent.com/xiaoshan-shuma/full-video-motion-workflow/main/install.sh | bash
```

安装完成后重启或刷新 Codex。

### 最简单的调用方式

把素材放进当前 ChatCut 项目后，说：

```text
请使用 full-video-motion-workflow 完成这条视频，采用奶油拼贴风。
读取当前 ChatCut 项目，先分析整条口播并建立完整分镜，再制作独立动效、B-roll、转场和音效并组装到 ChatCut。
保留已有字幕和原配音，动效必须真透明，不能挡脸；除非我说导出，否则只完成可编辑时间线。
```

也可以把“奶油拼贴风”换成“深海军蓝科技风”或 `navy-tech`。

## 3. 风格选择：二选一

每次制作前选择一种主风格，并在整条视频中保持统一，不自动混用。

### A. 深海军蓝科技风（`navy-tech`）

适合 AI 工具、软件操作、自动化流程、数据和科技观点。

- 深海军蓝背景和面板；青蓝用于结构线、路径和高光。
- 粉色突出关键词和主要动作，黄色只做少量第二重点。
- 左侧放中文大标题，右侧用 UI、流程、数据或卡片解释内容。
- 面板以方角或轻微圆角为主；顶部只保留一条细、不透明的全局进度条。
- 标题滑入、UI 分层展开、数字平滑滚动、卡片翻页；转场使用 ease-out、ease-in-out 或轻微 spring。

### B. 奶油拼贴信息图风（`cream-collage`）

适合内容创作、方法拆解、工作流说明、步骤卡、编辑工作台和产品发布信息图。

- 透明底叠加优先；需要底色时使用奶油白 `#F7F0E5`。
- 主文字为深咖啡 `#3B352F`；强调色使用珊瑚橙 `#E58B76`、鼠尾草绿 `#A9B8A0`、柔雾粉 `#D99A9B`、奶油黄 `#E9C875`。
- 使用 14–18px 圆角、细深咖啡描边和轻纸张阴影；避免深蓝、纯黑大底、厚重黑盒和霓虹渐变。
- 默认左侧一个大关键词，中间保留人物口播区，右侧 2–3 个小卡片、标签、时间轴或纸张图形。
- 长文案按语义拆成 2–4 个画面；每个画面只保留一个核心重点，小字优先拆成两行。
- 标题轻微滑入，卡片错开 0.12–0.20 秒依次下落或弹出；切换用淡出、同向擦拭或卡片滑移。

未指定风格时默认使用 `navy-tech`。用户给出参考图或参考视频时，以参考内容为最高优先级，但仍需选择一套主风格并保持统一。

## 4. 自动执行规则

以下说法会触发完整制作流程：

- “生成视频”
- “开始做”
- “继续做”
- “按这个素材完成”
- “调用这个 Skill 做完整视频”

只有用户明确说“先设计”“先给我分镜”“先不要开始”时，才只输出方案，不修改 ChatCut。

除非用户明确要求导出，否则完成标准是 ChatCut 中可播放、可继续编辑的时间线。

## 5. 三个工具的分工

### HyperFrames

制作网页感、产品感和交互感内容：产品界面、鼠标、按钮、滚动、翻页、流程图、步骤卡和浏览器式 B-roll。

### Remotion

制作跨镜头和可复用动画：全局进度条、标题、关键词、数字滚动、人物圆形画中画、复杂转场和多镜头时间编排。

### ChatCut

负责最终可编辑组装：导入素材、排列轨道、缩放裁剪、定位层级、保留字幕和配音、处理 BGM/音效、逐镜头预览和导出。

## 6. 标准制作流程

### 第一步：盘点项目

确认当前项目、时间线、画布、帧率、总时长，以及 A-roll、B-roll、图片、已有动效、字幕、BGM 和音效。先读取和确认，不先覆盖用户已有内容。

### 第二步：理解整条口播

完整理解内容后再拆分视觉段落。每段记录：口播与语义目的、起止时间、画面类型、进入动作、主体运动、关键词 cue、稳定态、退出动作、转场方向、人物/字幕/进度条安全区和音效需求。

### 第三步：制作独立动效

常见独立素材包括：

```text
progress-bar
title
keyword-highlight
metric-counter
feature-card
logo-drop
speaker-bubble
scene-transition
```

叠加到人物上的素材必须是真透明 WebM VP8/VP9 或 ProRes 4444，不能用黑底 MP4 冒充透明。不要把整条视频合成一个难以调整的大视频。

### 第四步：设计转场

- 左移接左移，右移接右移。
- 推近接局部放大或 UI 展开。
- 卡片出现接卡片翻页或同色延展。
- 数据滚动接数字/图表变化。
- 人物全屏接人物圆形缩小。

默认转场长度为 6–12 帧，使用 ease-out、ease-in-out 或轻微 spring，避免硬闪、黑场、突然反向和无意义弹跳。

### 第五步：导入并组装 ChatCut

1. 主视频轨：人物 A-roll 或主画面。
2. B-roll 轨：全屏录屏、产品画面和不透明视频。
3. 上层轨：透明标题、卡片、数字、Logo 和装饰。
4. 最高层：全片唯一的顶部进度条。
5. 画中画：放在不挡脸、不挡字幕的角落。
6. 最后加入必要的转场、音效和 BGM 调整。

### 第六步：处理声音

- 保留原配音，不擅自重新生成。
- 人声加强只作用于口播素材。
- BGM 独立处理，可从约 `-30 dB` 作为人声下的起点。
- `Mouse Click` 只用于主要点击、翻页或段落切换。
- `whoosh` 用于明确平移/翻页，`pop` 用于少量卡片弹出。
- 不要每出现一个小元素就加一次音效；音效之间要留出尾音。

### 第七步：逐镜头验收

至少检查首帧、进入动作、关键词出现时刻、稳定态、转场中段、退出和下一镜头首帧。重点确认：

- 没有黑底、黑帧或透明失效。
- 没有挡住脸、字幕或主要信息。
- 没有乱码、重叠、裁切或错误换行。
- 顶部进度条细、不透明、连续且只有一条。
- 转场自然，运动方向一致。
- BGM 没有盖住人声，音效不过量。
- 原有字幕、配音和已确认画面没有被改动。

## 7. 推荐的分镜记录格式

```text
镜头编号：S01
起止时间：00:00.000 - 00:04.800
风格：navy-tech / cream-collage
口播内容：这一段实际说了什么
观众任务：这一段要让观众理解什么
画面类型：A-roll / B-roll / 透明叠加 / 圆形画中画 / CTA
进入：从哪里、以什么速度进入
发展：画面中持续发生什么
重点：哪个词或数字在什么时候强调
稳定态：观众最终看到的完整画面
退出：如何离开当前镜头
转场：方向、类型、帧数
位置：标题、卡片、人物和字幕安全区
音频：BGM 音量、音效名称和锚点
素材：HyperFrames 或 Remotion，是否需要 Alpha
```

## 8. 推荐文件与命名

```text
S01-title-alpha.webm
S01-ui-card-alpha.webm
S01-metric-counter-alpha.webm
S01-transition-alpha.webm
S02-speaker-bubble-alpha.webm
progress-bar-global-alpha.webm
scene-plan.json
style-tokens.json
chatcut-import-plan.json
audio-plan.json
qa-report.md
```

`style-tokens.json` 必须记录本次选用的 `navy-tech` 或 `cream-collage`，以及对应色板、字体、阴影和间距。

## 9. 反复迭代规则

当用户说“再细一点”“不要挡脸”“换颜色”“转场不自然”时：

1. 找到具体镜头和具体素材。
2. 只替换该素材或该段设置。
3. 保留其他已确认内容。
4. 重新检查该镜头前后各一段。

不要因为一个素材有问题就覆盖整条时间线。

## 10. 交付规则

默认只完成 ChatCut 中可播放、可继续编辑的时间线，不自动导出。

只有用户明确说“导出”或“保存到桌面”时，才执行导出，并确认文件真实生成后再交付。

## 11. 给别人分享

仓库是公开的 MIT 开源项目：

https://github.com/xiaoshan-shuma/full-video-motion-workflow

别人可以用一句命令安装：

```bash
curl -fsSL https://raw.githubusercontent.com/xiaoshan-shuma/full-video-motion-workflow/main/install.sh | bash
```

安装后调用：

```text
使用 full-video-motion-workflow，采用奶油拼贴风。
```

或者：

```text
使用 full-video-motion-workflow，采用 navy-tech 深海军蓝科技风。
```

## 12. 一句话版本

用 `full-video-motion-workflow` 把当前 ChatCut 素材做成完整成片：先分析整条口播和分镜，再按用户选择的科技蓝或奶油风制作 HyperFrames/Remotion 独立动效、B-roll 和转场，所有叠加素材真透明，保留已有字幕和原配音，避开脸部与字幕，最后导入 ChatCut 逐镜头检查；除非用户明确说导出，否则不导出。
